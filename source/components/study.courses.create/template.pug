// start /components/study.courses.create/

script( src="https://cdn.jsdelivr.net/npm/vue@2" )
script( src="https://unpkg.com/vuex@3" )

script( src="https://unpkg.com/vue-select@latest" )
link( rel="stylesheet" href="https://unpkg.com/vue-select@latest/dist/vue-select.css" )

script( src="https://cdn.jsdelivr.net/npm/vue2-datepicker@3.9.0/index.min.js" )
link( rel="stylesheet" href="https://cdn.jsdelivr.net/npm/vue2-datepicker@3.9.0/index.min.css" )

script( src="https://cdn.jsdelivr.net/npm/vue-scrollto" )
script( src="https://cdnjs.cloudflare.com/ajax/libs/velocity/1.2.3/velocity.min.js" )

script( src="/components/study.courses.create/store.js" )

script
  | window.studyCourseCreatePaths = {
  |   addCompany: '/components/study.courses.create/addCompany.json',
  | };

//- push style and script file to the end of the page
if componentsArray
  - componentsArray[ componentsArray.length ] = 'study.courses.create'
else
  - var componentsArray = [ 'study.courses.create' ]
  
.b-study-courses-create.ph-block

  .ph
    div
    div
    div
    div
    div

  .ph-content

    #studyCoursesCreateWizard

// end /components/study.courses.create/